
// Define las props que recibe el componente Header.
// isLoggedIn: indica si el usuario ha iniciado sesión, lo cual permite mostrar u ocultar ciertos elementos del header como "Cerrar sesión" o "Mi perfil".
interface HeaderProps {
    isLoggedIn: boolean;
}
